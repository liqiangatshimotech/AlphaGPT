# 短周期事件策略实验结果

本轮按“山寨币可能在 15 分钟内失去流动性”的假设改造研究流程：不使用长期均线，使用完整 3m/5m OHLCV 计算信号，执行和风控仍按每分钟检查。

信号候选是：

- `impulse`：当前实体收益和 K 线压力；
- `reversal`：上一根短周期收益的反转；
- `volume_breakout`：压力信号乘以成交量突增。

训练/验证预注册了 3/5 根聚合 K 线的持有期。入场在信号 K 线结束后下一分钟开盘，短周期最大持有分别为 9、15、15、25 分钟；加入最短持有、冷却、止损和流动性下降退出。训练阈值固定为训练地址信号的 95% 分位，退出阈值为 50% 分位。

## 结果

| 候选 | 训练净收益 | 验证净收益 | 验证可用 |
|---|---:|---:|---|
| 3m impulse / 3 根 | -95.23% | -95.03% | 是 |
| 3m impulse / 5 根 | -95.04% | -95.30% | 是 |
| 3m reversal / 3 根 | 116.27% | -15.76% | 是 |
| 3m reversal / 5 根 | 123.56% | 12.07% | 是 |
| 3m volume breakout / 3 根 | -95.29% | -95.33% | 是 |
| 5m reversal / 3 根 | -95.02% | -54.71% | 是 |
| 5m reversal / 5 根 | -95.21% | -51.89% | 是 |

3m reversal / 5 根的验证净收益看起来为正，但它集中在少数 token：验证阶段 10/34 个训练地址有交易，单个 token 贡献超过总绝对 token PnL 的 25%。加入 token 覆盖率和集中度硬门槛后，**没有候选被选中**。

## 结论

短周期信号比长期均线更符合山寨币的生命周期，但现有数据和成本假设下仍没有可部署候选。正收益候选依靠单币事件，不能称为跨 token alpha；其他候选大量因手续费、冲击成本和现金耗尽而接近 `-95%`。

本轮还发现，数据库中的流动性/FDV 可能是当前快照回填，不能当作历史真实撤池风险。执行报告因此把流动性风险标为研究假设，而不是已验证成交事实。

进一步检查当前库的 48 个 token：每个 token 的 `liquidity` 不同值数量中位数只有 6 个，最少为 1 个；很多长时间序列的流动性值完全不变。这与 Birdeye OHLCV 不返回历史池深度时用当前 token 快照回填的旧逻辑一致。现已修改采集器：缺少历史 `liquidity/fdv` 时写入 `NULL`，不再复制当前值到旧 K 线。已有旧记录不会被伪造修复，后续需要重新采集或使用独立的实时深度数据。

下一步应先解决数据和执行条件：

1. 用真实 API 返回的 3m/5m 完整 OHLCV，而不是从稀疏历史点抽样。
2. 保存实时流动性、报价深度和撤池事件，避免用当前状态回填历史。
3. 预先固定每日最大交易次数和单币最大贡献。
4. 以新增、此前未查看过的行情做前向盲测。
5. 只有简单基线在地址留出和盲测都跑赢持有基线，才恢复 AlphaGPT 训练。

实验入口：`python -m model_core.short_cycle --out logs/short-cycle-03`。
结果报告：`logs/short-cycle-03/report.json`。本轮没有部署或覆盖任何现有策略。
