# AlphaGPT 新模型训练全过程报告

## 1. 目标

本项目要从 meme token 的 OHLCV 行情中生成可执行的交易因子公式。验收目标不是训练集收益最高，而是找到在未参与选择的时间段和 token 上仍然有效的公式，并且在真实成本、滑点、流动性、单仓位限制下可交易。

一个候选模型只有同时满足以下条件才可以进入运行器：

- 公式通过 `StackVM` 语法与数值检查；
- 扣除手续费和流动性冲击后收益为正；
- 验证集和测试集方向一致，并且能够跑赢简单持有基线；
- 不是依靠单个 token 的极端收益；
- 换手率、最大回撤、交易次数和缺失行情风险可接受；
- 能在生产运行器中复现同样的特征、阈值和执行时序。

## 2. 初始训练与分支

已有修复提交 `44a295b` 已推送到 fork。随后创建训练分支：

`train/new-model-20260919`

初始入口是 `model_core.engine`。它从完整词表自由采样，非法公式统一奖励 `-5`。启动后发现：

- 数据为 41 个 token、6 个特征、约 10921 个时间点；
- 单步约 25 秒，预计需要数小时；
- 平均奖励约 `-5.05`，最佳分数短暂变化但没有有效学习信号；
- 训练在第 4 步停止，没有把这轮当成成功训练。

主要原因是大部分公式操作数不足或栈无法归约到单一结果，策略梯度几乎只学习非法公式惩罚。

## 3. 因果公式搜索方案

新增 `model_core/research_train.py`，先不覆盖生产策略文件，单独保存快照、日志、模型权重和报告。

方案包括：

1. 用栈深度和剩余长度做 action mask，确保每个采样公式合法。
2. 禁用使用整段时间统计量的 `JUMP` 算子。
3. 对缺失 OHLCV 字段保留 `observed` 掩码。
4. 上市或断档后设置 20 根 K 线暖身，避免 FOMO/DEV 首根伪信号。
5. 使用时间段 60%/20%/20% 切分，训练集选候选，验证集选择，测试集只评估一次。
6. 奖励中加入持有基线、回撤、换手和 token 覆盖率。
7. 关闭 LoRD 做基础对照，避免把正则化问题和搜索问题混在一起。

后来又把因果标准化改成按连续行情段在线计算，避免长缺口之前的统计量污染新行情。

## 4. 因果策略梯度实验

### 4.1 `research-causal-01`

配置：30 步 × 64 公式，随机种子 42，学习率 `1e-4`。共生成 1920 个通过语法检查的公式。

| 策略 | 训练净损益 | 验证净损益 | 测试净损益 | 测试累加回撤 |
|---|---:|---:|---:|---:|
| 选中公式 | 0.314584 | 0.272650 | 0.004160 | 0.041634 |
| 持有可交易资产 | 0.144673 | 0.083190 | 0.122932 | 0.030213 |

选中公式为：

```text
[1, 11, 15, 15, 10, 1, 7, 11, 4, 12, 10, 8]
```

虽然验证集为正，但测试集接近零，且低于持有基线，因此淘汰。

### 4.2 `research-causal-02`

配置为 300 步 × 256 公式。运行至约第 146 步时停止：

- 合法公式率保持 100%；
- 平均奖励从约 `-7.5` 改善到约 `-1`；
- 最佳训练奖励停在约 `0.246`；
- 几乎每个样本都是唯一公式，策略仍未收敛；
- 因为上市边界伪信号和运行时间过长，没有生成完整报告。

这轮不作为验收结果。

### 4.3 `research-causal-03`

修复缺失掩码、上市暖身、持有基线奖励和换手惩罚后，运行 100 步 × 64 公式。

| 数据集 | 策略净损益 | 持有基线 | 超额收益 | 累加回撤 | 换手单位 | 暴露 |
|---|---:|---:|---:|---:|---:|---:|
| 训练 | 0.500223 | 0.144673 | 0.355549 | 0.192639 | 5960 | 1.66% |
| 验证 | 0.184709 | 0.083190 | 0.101518 | 0.014496 | 1120 | 0.73% |
| 测试 | 0.023134 | 0.122932 | -0.099798 | 0.035272 | 638 | 0.41% |

测试集跑输持有基线。更细的 token 分解还显示，策略在验证和测试阶段主要依赖 41 个 token 中的单个 token，其余 token 几乎不交易。该轮淘汰。

## 5. 遗传公式搜索

由于策略梯度搜索方差太大，新增 `model_core/genetic_search.py`：表达式树表示、精英保留、子树交叉、算子/叶节点变异、多时间窗口评分，并加入 Top-1 执行约束。

### `genetic-walkforward-04`

这是第一次遗传搜索诊断，使用 128 个候选、12 代、16 个精英。测试净损益 `-0.000052`，持有基线 `0.003371`，超额收益 `-0.003423`，token 中位超额收益 `-0.054605`。未通过验收。

### `genetic-walkforward-05`

修复了随机初始化仍可能采到 `JUMP` 的问题。最终测试净损益 `-0.02725`，持有基线 `0.00337`，超额收益 `-0.03062`，奖励 `-0.05091`。验证窗口也未达到 token 覆盖门槛，淘汰。

### `genetic-walkforward-06`

加入单仓位状态机：Top-1、最短持有 5 根、冷却 3 根、无信号状态。

- 训练最优奖励约 `-0.01423`；
- 验证窗口均未达到覆盖门槛；
- 测试净损益 `-0.01887`；
- 持有基线 `0.00337`；
- 超额收益 `-0.02224`；
- 风险惩罚后奖励 `-0.03608`。

状态机减少了无意义的逐根换仓，但没有产生稳定的跨窗口 alpha。

## 6. 独立地址留出和执行回测

此前的时间切分和遗传回测仍存在两个口径问题：

- 旧 `tradable` 掩码包含未来 K 线是否存在，可能把未来数据用于当前买入筛选；
- 多 token PnL 使用 token 数均值，容易被单个 token 的异常收益稀释；
- 旧回测没有真实的现金耗尽和缺失成交处理。

因此新增 `model_core/holdout_execution.py` 和 `model_core/holdout_benchmark.py`：

- 信号在 `t`，下一根开盘成交；
- 成交时扣手续费和流动性冲击；
- 只有一笔持仓；
- 未来缺失成交数据会标记不可用，不悄悄记成零收益；
- 现金低于初始本金 5% 后停止开仓并标记破产；
- 按稳定 hash 划分 31 个训练地址和 10 个留出地址；
- 预注册 `NEG_RET`、`NEG_PRESSURE`、组合信号和 `RET` 四个基线，阈值只从训练地址的 80%/90% 分位数估计。

在 1 分钟数据、15 根持有期的地址留出结果：

| 公式 | 阈值 | 净损益 | 毛损益 | 成本 | 交易次数 | 破产 |
|---|---:|---:|---:|---:|---:|---:|
| NEG_RET | 80% | -0.9501 | 0.1494 | 1.0995 | 267 | 是 |
| NEG_RET | 90% | -0.9502 | 0.1383 | 1.0885 | 290 | 是 |
| NEG_PRESSURE | 80% | -0.9500 | 0.0790 | 1.0290 | 248 | 是 |
| NEG_RET_PRESSURE | 80% | -0.9507 | 0.1102 | 1.0609 | 280 | 是 |
| RET | 80% | -0.9506 | -0.1236 | 0.8271 | 237 | 是 |

这说明在 1 分钟周期和当前约 0.6% 单边基础费率下，交易成本主导结果。此前对 15 根收益的粗测没有使用相同的现金、成交和破产约束，不能作为有效结论。

还做了 15 分钟对齐采样诊断，但这不是完整 OHLC 聚合，缺失成交较多，不能直接用于训练或部署。

## 7. 最终结论

截至本报告：

- 没有任何新公式通过验证和测试验收；
- 没有替换 `best_meme_strategy.json`；
- 所有正向训练集或验证集结果都在更严格的测试、地址留出或真实现金执行口径下失效；
- 继续增加 AlphaGPT 步数或 LoRD 强度没有证据能解决问题；
- 当前最大瓶颈是数据频率、交易成本、缺失行情和跨 token 泛化，而不是 Transformer 容量。

## 8. 后续建议

1. 从 1 分钟原始行情生成真正连续的 15 分钟/30 分钟 OHLCV，而不是抽取对齐时刻的单根记录。
2. 为 15/30 分钟周期重新定义未来收益标签，持有期与标签严格一致。
3. 把每日最大交易次数、冷却和最大单币贡献作为硬约束。
4. 在冻结公式和阈值后，用数据库时间末端新增行情建立真正盲测；当前历史段和 41 个地址都已经被查看过，地址留出只能算稳健性诊断。
5. 先让简单基线在独立地址和前向数据上跑赢持有基线，再恢复遗传搜索和 AlphaGPT 微调。
6. 新模型通过验收前，保持现有运行策略不变，不把研究公式直接部署。

相关文件：

- [MODEL_TRAINING_PLAN.md](MODEL_TRAINING_PLAN.md)
- [MODEL_TRAINING_RESULTS.md](MODEL_TRAINING_RESULTS.md)
- [HOLDOUT_BENCHMARK_RESULTS.md](HOLDOUT_BENCHMARK_RESULTS.md)
- `logs/research-causal-01/`
- `logs/research-causal-03/`
- `logs/genetic-walkforward-06/`
- `logs/holdout-baselines-03/`

