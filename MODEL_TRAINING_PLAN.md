# 新模型训练目标与方案

## 目标

训练一个能够在 meme token 历史行情上生成可回测交易因子的 AlphaGPT 模型。目标不是让训练集收益最大，而是找到在未见时间段和未见 token 上仍然有效、可交易、风险可控的公式。

本轮模型需要同时满足：

- 公式合法，能够被 `StackVM` 稳定执行；
- 扣除手续费、滑点和流动性影响后仍有正向表现；
- 在训练集、验证集、测试集之间保持稳定，而不是只拟合某一段行情；
- 控制最大回撤、换手率和低流动性交易风险。

## 当前问题

上一轮 `training_history.json` 显示平均奖励几乎一直为 `-5.0`。这说明策略梯度主要收到“非法公式”的惩罚，模型尚未获得足够的有效公式样本。主要原因是模型从完整词表自由采样，容易生成操作数不足、栈无法归约到单一结果的公式。

因此，本轮先关注训练是否产生有效公式和稳定奖励，再比较收益率。单纯增加模型层数、训练步数或正则强度不能解决这个问题。

## 训练方案

### 阶段一：数据与执行检查

1. 检查行情时间线、缺失值、正价格和可交易掩码。
2. 按时间切分训练集、验证集和测试集，避免未来数据泄漏。
3. 验证每个特征和算子都能在 `StackVM` 中执行，并统计非法公式比例。
4. 用 `RET`、`LIQ_SCORE` 等单特征公式建立基线。

### 阶段二：合法公式搜索

1. 根据当前栈深度对 token 采样做 action mask。
2. 栈深度为零时禁止需要操作数的算子。
3. 公式结束时要求栈深度恰好为一。
4. 对非法公式保留明确原因统计，避免所有错误都折叠成同一个奖励。

### 阶段三：奖励与优化

奖励综合以下指标：风险调整收益、年化收益、最大回撤、换手率、手续费和滑点。先关闭 LoRD 做基础训练对照；确认基础训练能产生有效奖励后，再比较 LoRD 是否改善泛化。

建议初始配置：

```text
batch size: 1024
learning rate: 1e-4
train steps: 10000
gradient clipping: 1.0
```

### 阶段四：验证与选型

每轮训练保存：

- 最佳公式及词表版本；
- 训练、验证、测试集的 Sharpe、收益、最大回撤、换手率；
- 合法公式比例和奖励分布；
- 不同 token 子集和不同时间窗口的稳定性。

只有验证集表现稳定的公式才进入最终测试。测试集只使用一次，避免反复调参造成隐性过拟合。

## 本轮验收标准

- 合法公式比例显著高于上一轮；
- 平均奖励不再长期停留在非法公式惩罚值；
- 验证集扣费后收益为正，且最大回撤和换手率在可接受范围；
- 测试集表现与验证集方向一致；
- 最终策略文件能够被运行器加载并通过词表兼容性检查。

本文件记录的是训练目标和实验方法；具体结果以本分支生成的日志、`training_history.json` 和策略文件为准。

## 2026-09-19 实际执行修订

原入口尚未实现上述方案，首次启动的旧训练已停止，不能称为完成新模型训练。
旧公式事后按时间切段仍属于样本内诊断；此前把累加对数收益回撤解释成账户回撤百分比不准确。

新增隔离入口 `python -m model_core.research_train`：

- 栈深度与剩余长度共同限制 token，禁用使用未来统计量的 JUMP。
- 实验特征采用截至当前时点的累积统计，修复 FOMO 首行循环取末行的问题。
- 时间比例 60/20/20，训练热身 20 行，每段末尾剔除两行，防止标签跨段。
- 仅训练段参与梯度；训练排名前 10 个公式进入验证段，选定后只评估一次测试段。
- 奖励为固定名义本金口径的净损益减最大累加回撤；不足 5 次入场记 -1。
- 单边基础费用 0.006，冲击近似 1000/流动性（上限 0.05），流动性门槛 500000，段末扣平仓费。
- 冒烟实验：30 步 × 64 公式，学习率 1e-4，熵系数 0.01，梯度裁剪 1，种子 42，关闭 LoRD 和 dropout。
- 保存行情快照、模型/优化器权重、逐步 JSONL 日志和评估报告，不覆盖现有运行策略。

局限：历史测试段已在前面对话中查看过，不能作为全新盲测；币种池仍按全历史覆盖筛选，存在生存偏差；缺失行情、真实成交和账户资金约束尚未完整模拟。新特征定义与运行器不同，实验公式不能直接部署。此轮不计算年化 Sharpe，也不承诺收益。
